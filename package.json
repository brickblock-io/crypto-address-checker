{
  "name": "crypto-address-checker",
  "version": "1.1.2",
  "module": "build/index.js",
  "repository": "git@github.com:brickblock-io/crypto-address-checker.git",
  "author": "mattgstevens <mattgstevens@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build:comment": "this tool is faster than using the babel plugin, and is useful when you do not need babel / want to remove flowtypes directly as a step for: browserify / rollup /webpack",
    "build": "flow-remove-types --pretty index.js --out-dir build/",
    "preversion": "yarn build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015-node5": "^1.2.0",
    "babel-preset-flow": "^6.23.0",
    "prettier": "^1.8.2"
  },
  "dependencies": {
    "bs58check": "^2.1.0",
    "crypto-js": "^3.1.9-1"
  }
}
